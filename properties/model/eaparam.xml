<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eaparam">
  <select id="getTableAll" parameterType="map" resultType="hashmap">select * from eaparam</select>
  <select id="getTableAllCount" parameterType="map" resultType="int">select count(*) from eaparam</select>
  <select id="getTableByPk" parameterType="map" resultType="hashmap">select * from eaparam where 1=1 and PARAMID= #{PARAMID} and PARAMID= #{PARAMID} and PARAMKEY= #{PARAMKEY} and PARAMKEY= #{PARAMKEY} and PARAMID= #{PARAMID} and PARAMID= #{PARAMID} and PARAMKEY= #{PARAMKEY} and PARAMKEY= #{PARAMKEY} </select>
  <delete id="deleteTableByPk" parameterType="map">delete eaparam where 1=1 and PARAMID= #{PARAMID} and PARAMID= #{PARAMID} and PARAMKEY= #{PARAMKEY} and PARAMKEY= #{PARAMKEY} and PARAMID= #{PARAMID} and PARAMID= #{PARAMID} and PARAMKEY= #{PARAMKEY} and PARAMKEY= #{PARAMKEY} </delete>
</mapper>