<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eacode">
  <select id="getTableAll" parameterType="map" resultType="hashmap">select * from eacode</select>
  <select id="getTableAllCount" parameterType="map" resultType="int">select count(*) from eacode</select>
  <select id="getTableByPk" parameterType="map" resultType="hashmap">select * from eacode where 1=1 and CODEID= #{CODEID} and CODEID= #{CODEID} and CODEID= #{CODEID} and CODEID= #{CODEID} </select>
  <delete id="deleteTableByPk" parameterType="map">delete eacode where 1=1 and CODEID= #{CODEID} and CODEID= #{CODEID} and CODEID= #{CODEID} and CODEID= #{CODEID} </delete>
</mapper>