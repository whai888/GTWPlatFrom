<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eamenupart">
  <select id="getTableAll" parameterType="map" resultType="hashmap">select * from eamenupart</select>
  <select id="getTableAllCount" parameterType="map" resultType="int">select count(*) from eamenupart</select>
  <select id="getTableByPk" parameterType="map" resultType="hashmap">select * from eamenupart where 1=1 and PARTID= #{PARTID} and PARTID= #{PARTID} and PARTID= #{PARTID} and PARTID= #{PARTID} </select>
  <delete id="deleteTableByPk" parameterType="map">delete eamenupart where 1=1 and PARTID= #{PARTID} and PARTID= #{PARTID} and PARTID= #{PARTID} and PARTID= #{PARTID} </delete>
</mapper>