<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eamenu">
  <select id="getTableAll" parameterType="map" resultType="hashmap">select * from eamenu</select>
  <select id="getTableAllCount" parameterType="map" resultType="int">select count(*) from eamenu</select>
  <select id="getTableByPk" parameterType="map" resultType="hashmap">select * from eamenu where 1=1 and MENUID= #{MENUID} and MENUID= #{MENUID} and MENUID= #{MENUID} and MENUID= #{MENUID} </select>
  <delete id="deleteTableByPk" parameterType="map">delete eamenu where 1=1 and MENUID= #{MENUID} and MENUID= #{MENUID} and MENUID= #{MENUID} and MENUID= #{MENUID} </delete>
</mapper>