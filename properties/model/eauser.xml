<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eauser">
  <select id="getTableAll" parameterType="map" resultType="hashmap">select * from eauser</select>
  <select id="getTableAllCount" parameterType="map" resultType="int">select count(*) from eauser</select>
  <select id="getTableByPk" parameterType="map" resultType="hashmap">select * from eauser where 1=1 and USERID= #{USERID} and USERID= #{USERID} and USERID= #{USERID} and USERID= #{USERID} </select>
  <delete id="deleteTableByPk" parameterType="map">delete eauser where 1=1 and USERID= #{USERID} and USERID= #{USERID} and USERID= #{USERID} and USERID= #{USERID} </delete>
</mapper>