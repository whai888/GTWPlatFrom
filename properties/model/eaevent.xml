<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eaevent">
  <select id="getTableAll" parameterType="map" resultType="hashmap">select * from eaevent</select>
  <select id="getTableAllCount" parameterType="map" resultType="int">select count(*) from eaevent</select>
  <select id="getTableByPk" parameterType="map" resultType="hashmap">select * from eaevent where 1=1 and EVENTID= #{EVENTID} and EVENTID= #{EVENTID} and EVENTID= #{EVENTID} and EVENTID= #{EVENTID} </select>
  <delete id="deleteTableByPk" parameterType="map">delete eaevent where 1=1 and EVENTID= #{EVENTID} and EVENTID= #{EVENTID} and EVENTID= #{EVENTID} and EVENTID= #{EVENTID} </delete>
</mapper>