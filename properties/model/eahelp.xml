<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eahelp">
  <select id="getTableAll" parameterType="map" resultType="hashmap">select * from eahelp</select>
  <select id="getTableAllCount" parameterType="map" resultType="int">select count(*) from eahelp</select>
  <select id="getTableByPk" parameterType="map" resultType="hashmap">select * from eahelp where 1=1 and HELPID= #{HELPID} and HELPID= #{HELPID} and HELPID= #{HELPID} and HELPID= #{HELPID} </select>
  <delete id="deleteTableByPk" parameterType="map">delete eahelp where 1=1 and HELPID= #{HELPID} and HELPID= #{HELPID} and HELPID= #{HELPID} and HELPID= #{HELPID} </delete>
</mapper>